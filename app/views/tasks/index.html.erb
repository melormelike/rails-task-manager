<ul>
  <% @tasks.each do |task| %>
  <li><%= task['title'] %> <%= task['details'] %> - <%= task[:completed] %> <%= link_to "see task", task_path(task)%> </li>
  <%end%>
</ul>

<p>
<%= link_to "add a new task", new_task_path%></p>
